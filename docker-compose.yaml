version: "3.8"
services:
  mysql:
    build: ./db/
    image: trio-db:v2
    environment:
      - MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
  flask-app:
    image: trio-app
    build: ./flask-app/
    environment:
      - DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      - mysql
  trio-nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - target: 80
        published: 80
    depends_on:
      - flask-app

